@model IEnumerable<WebsiteBanSach.Models.CongTyPhatHanh>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenCongTy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoLuongSach)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenCongTy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoLuongSach)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaCongTyPhatHanh }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaCongTyPhatHanh }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaCongTyPhatHanh })
        </td>
    </tr>
}

</table>
