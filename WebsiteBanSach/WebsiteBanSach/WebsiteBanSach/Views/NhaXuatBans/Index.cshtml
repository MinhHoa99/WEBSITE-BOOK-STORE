@model IEnumerable<WebsiteBanSach.Models.NhaXuatBan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenNXB)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoLuongSach)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenNXB)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoLuongSach)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaNhaXuatBan }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaNhaXuatBan }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaNhaXuatBan })
        </td>
    </tr>
}

</table>
